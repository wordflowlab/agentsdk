---
title: Skills 系统
description: 动态知识库注入和领域专业化能力
navigation:
  icon: i-lucide-zap
---

# Skills 系统

Skills 是 AgentSDK 的动态知识库注入系统，允许您为 Agent 添加领域专业知识、工作流程和最佳实践，而无需修改系统提示词或重新训练模型。

## 🎯 核心概念

Skills 系统通过 **Injector（注入器）** 在运行时动态加载和注入知识库内容，使 Agent 能够根据上下文自动激活相关技能。

```
用户消息 → Trigger检测 → 加载Skills → 注入知识库 → Agent处理
    ↓
"写小说"  →  keyword触发  → consistency-checker.md → 注入到Prompt → Claude思考
```

### Skills vs 其他扩展方式

| 特性 | Skills | Custom Tools | Slash Commands |
|------|--------|--------------|----------------|
| **用途** | 知识库注入 | 执行操作 | 快捷指令 |
| **触发方式** | 自动/关键词 | Agent主动调用 | 用户输入 |
| **内容形式** | Markdown文档 | Go代码 | 命令脚本 |
| **适用场景** | 领域知识、规范 | 文件操作、API | 快速启动 |

## 📦 Skill 定义

### Skill 文件格式

Skill 使用 Markdown 文件定义，包含 YAML 元数据和知识库内容：

```markdown
---
name: consistency-checker
description: 写作过程中自动检查角色行为、世界规则和时间线一致性
allowed-tools: ["Read", "Grep", "Write"]
triggers:
  - type: keyword
    keywords: ["一致性", "检查", "consistency"]
  - type: context
    condition: "during /write"
  - type: always
---

# 一致性检查指南

## 检查清单

### 角色一致性
- 性格特征是否前后一致
- 对话风格是否符合人设
- 行为动机是否合理

### 世界规则
- 魔法体系规则是否矛盾
- 科技水平是否统一
- 地理设定是否一致

### 时间线
- 事件顺序是否合理
- 角色年龄是否匹配
- 季节天气是否对应

## 最佳实践

1. **定期检查**：每完成一章后进行全面检查
2. **增量验证**：写作过程中实时验证关键设定
3. **记录规则**：维护世界观设定文档
```

### 元数据字段说明

| 字段 | 类型 | 必需 | 说明 |
|------|------|------|------|
| `name` | string | ✅ | Skill唯一标识符 |
| `description` | string | ✅ | Skill功能描述 |
| `allowed-tools` | []string | ❌ | 允许使用的工具列表 |
| `triggers` | []Trigger | ✅ | 触发条件列表 |

## 🔔 触发机制

Skills 支持四种触发方式，可以单独使用或组合使用。

### 1. 关键词触发（keyword）

当用户消息包含特定关键词时自动激活：

```yaml
triggers:
  - type: keyword
    keywords: ["一致性", "检查", "consistency", "verify"]
```

**示例**：
```go
// 用户输入："帮我检查一下角色一致性"
// → 触发 consistency-checker Skill
```

### 2. 上下文触发（context）

在特定命令或状态下激活：

```yaml
triggers:
  - type: context
    condition: "during /write"
```

**适用场景**：
- 特定 Slash Command 执行期间
- 特定工作流程阶段
- Agent 处于特定模式

### 3. 总是激活（always）

无条件激活，始终注入知识库：

```yaml
triggers:
  - type: always
```

**适用场景**：
- 核心工作规范
- 必须遵守的安全准则
- 通用最佳实践

### 4. 文件模式触发（file_pattern）

当操作匹配特定文件路径时激活：

```yaml
triggers:
  - type: file_pattern
    pattern: "src/**/*.go"
```

**适用场景**：
- Go代码审查规范
- 特定项目结构规范
- 文件类型专用知识

### 组合触发

可以为一个 Skill 定义多个触发条件，任一条件满足即激活：

```yaml
triggers:
  - type: keyword
    keywords: ["测试", "test"]
  - type: file_pattern
    pattern: "**/*_test.go"
  - type: context
    condition: "during /test"
```

## ⚙️ Skills 注入流程

```
┌─────────────────┐
│  用户发送消息   │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Injector检测   │  遍历所有Skill的触发条件
│  触发条件       │
└────────┬────────┘
         │
         ▼
    ┌────┴────┐
    │  匹配？  │
    └────┬────┘
         │ 是
         ▼
┌─────────────────┐
│  加载Skill内容  │  从文件系统/OSS/S3加载
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  注入策略选择   │
└────────┬────────┘
         │
    ┌────┴────┐
    │ 模型支持 │
    │SystemPrompt?│
    └────┬────┘
         │
    ┌────┴────┐
    │   是    │   否
    ▼         ▼
┌────────┐ ┌────────┐
│注入到   │ │注入到   │
│System   │ │User    │
│Prompt   │ │Message │
└────┬───┘ └────┬───┘
     │          │
     └────┬─────┘
          ▼
    ┌─────────────┐
    │  发送给LLM  │
    └─────────────┘
```

### 注入策略

AgentSDK 根据模型能力自动选择注入位置：

**支持 System Prompt 的模型**（如 Claude、GPT-4）：
```
System Prompt:
  [原始系统提示词]

  ## Activated Skills

  ### consistency-checker
  [Skill内容]

User Message:
  [用户消息]
```

**不支持 System Prompt 的模型**（如部分国产模型）：
```
User Message:
  ## Knowledge Base

  ### consistency-checker
  [Skill内容]

  ---

  [用户消息]
```

### 多 Skill 组合

当多个 Skill 同时激活时，它们会按名称排序后依次注入：

```markdown
## Activated Skills

### coding-standards
[内容...]

### security-checklist
[内容...]

### test-guidelines
[内容...]
```

## 📁 Skills Package 组织

### 目录结构

```
workspace/
└── .claude/
    └── skills/
        ├── consistency-checker.md
        ├── coding-standards.md
        ├── security-review.md
        └── test-guidelines.md
```

### 加载配置

在 Agent 配置中指定 Skills Package 位置：

```go
ag, err := agent.Create(ctx, &types.AgentConfig{
    TemplateID: "assistant",
    SkillsPackageConfig: &types.SkillsPackageConfig{
        Path: "./workspace/.claude/skills",
    },
    // ... 其他配置
}, deps)
```

### 支持的存储方式

| 存储类型 | Path 格式 | 说明 |
|---------|-----------|------|
| 本地文件系统 | `./path/to/skills` | 开发测试推荐 |
| 阿里云OSS | `oss://bucket/prefix` | 生产环境推荐 |
| AWS S3 | `s3://bucket/prefix` | 跨区域部署 |
| HTTP(S) | `https://cdn.com/skills` | CDN分发 |

## 🎨 使用场景

### 1. 写作助手

```markdown
---
name: writing-coach
description: 提供写作技巧和风格建议
triggers:
  - type: keyword
    keywords: ["写作", "文章", "blog"]
---

# 写作最佳实践

## 结构
- 开头：吸引注意力的钩子
- 中间：逻辑清晰的论述
- 结尾：有力的总结

## 风格
- 使用主动语态
- 避免冗余表达
- 保持段落简短
```

### 2. 代码审查

```markdown
---
name: go-code-review
description: Go语言代码审查标准
allowed-tools: ["Read", "Grep"]
triggers:
  - type: file_pattern
    pattern: "**/*.go"
  - type: keyword
    keywords: ["review", "审查"]
---

# Go代码审查清单

## 错误处理
- 所有错误必须检查
- 使用errors.Is/As判断错误类型
- 返回有意义的错误信息

## 并发安全
- 检查共享数据的保护
- 避免goroutine泄漏
- 正确使用context
```

### 3. 安全审计

```markdown
---
name: security-audit
description: 安全漏洞检查清单
triggers:
  - type: always
---

# 安全审计指南

## OWASP Top 10
1. 注入攻击（SQL, XSS, Command）
2. 认证失效
3. 敏感数据泄露
...

## 检查要点
- 用户输入是否验证
- 密码是否加密存储
- API是否有权限控制
```

## 🚀 最佳实践

### 1. Skill 粒度设计

**推荐**：细粒度、单一职责
```
✅ coding-standards.md       (代码规范)
✅ security-checklist.md     (安全检查)
✅ test-guidelines.md        (测试指南)
```

**不推荐**：大而全的 Skill
```
❌ everything.md             (包含所有规范)
```

### 2. 触发条件选择

- **always**：仅用于核心规范和安全准则
- **keyword**：用于可选的专业知识
- **context**：用于特定流程阶段
- **file_pattern**：用于文件类型专用规范

### 3. 知识库内容组织

```markdown
# Skill 名称

## 概述
简短说明 Skill 的用途

## 核心规则
关键规则和原则

## 检查清单
可操作的步骤列表

## 示例
正确和错误的对比示例

## 参考资料
相关文档链接
```

### 4. 性能优化

- **控制大小**：单个 Skill 建议不超过 2000 token
- **延迟加载**：避免使用过多 `always` 触发
- **缓存策略**：频繁使用的 Skill 可以预加载

### 5. 版本管理

```
skills/
├── v1/
│   ├── coding-standards.md
│   └── security-checklist.md
└── v2/
    ├── coding-standards.md  (新版本)
    └── security-checklist.md
```

## 🔗 相关资源

- [自定义工具文档](/tools/builtin/custom) - 完整的 Skills 使用指南
- [Skills 示例](/examples/skills) - 实际应用案例
- [API 参考](/api-reference/skills) - Skills API 文档
- [最佳实践](/best-practices/skills) - 高级技巧和模式

## ⚡ 快速开始

```bash
# 1. 创建 Skills 目录
mkdir -p workspace/.claude/skills

# 2. 创建你的第一个 Skill
cat > workspace/.claude/skills/helper.md << 'EOF'
---
name: helper
description: 通用助手技能
triggers:
  - type: keyword
    keywords: ["帮助", "help"]
---

# 助手指南

我可以帮你：
- 回答问题
- 编写代码
- 审查文档
- 优化性能

请告诉我你需要什么帮助！
EOF

# 3. 在代码中启用 Skills
# 见上文"加载配置"部分
```

---

**下一步**：查看 [自定义工具文档](/tools/builtin/custom) 了解更多 Skills 使用技巧，或访问 [示例库](/examples/skills) 查看实际应用案例。
